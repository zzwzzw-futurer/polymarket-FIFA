{
  "name": "world-cup-data-portal",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "dev:live": "npm run sync:data && (npm run sync:data:watch & npm run dev)",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "sync:data": "node scripts/sync-worldcup-data.mjs",
    "sync:data:watch": "node scripts/sync-worldcup-data.mjs --watch"
  },
  "dependencies": {
    "@phosphor-icons/react": "^2.1.10",
    "@vitejs/plugin-react": "5.0.4",
    "react": "19.2.0",
    "react-dom": "19.2.0",
    "vite": "6.4.2"
  }
}
